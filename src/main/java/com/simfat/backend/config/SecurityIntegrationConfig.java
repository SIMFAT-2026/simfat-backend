package com.simfat.backend.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class SecurityIntegrationConfig {
    // Placeholder para integrar Spring Security (JWT, roles, filtros) en una fase posterior.
    // La API queda abierta para facilitar el desarrollo MVP y pruebas de frontend.
}

